import React from 'react';
import {SafeAreaView} from 'react-native';
import {Block, Text, NavBar, theme, Switch, Icon} from 'galio-framework';

// Molecules import
import RoomToolbar from "../../../organisms/rooms/room-toolbar";

export default class RoomListTemplate extends React.Component{
    render() {
        return (
            <Block>
                <Text>Rooms List</Text>
            </Block>
        )
    }
}